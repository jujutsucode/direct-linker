# 🤖 Direct Linker 
## Overview
Direct Linker is a FastApi Server for upload files and get direct links to download.

## Project Structure
- `app.py`: Server on FastApi.
- `templates/index.html`: View.
- `uploads/`: Uploaded files folder.

## Requirements
- `Python 3.6+`: based?
- `FastApi`: FastAPI is a modern, fast (high-performance), web framework
- `uvicorn`: An ASGI web server, for Python
- `Jinja2`: is a fast, expressive, extensible templating engine

## How to Usage ❓
1. Execute `python app.py` in project directory
2. Goto `http://127.0.0.1:5000` in your browser
3. Upload any file and get direct link to download
4. You are the best